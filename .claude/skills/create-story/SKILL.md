---
name: create-story
description: Create a Storybook story for an existing component.
---

1. Create `components/{ComponentName}.stories.tsx`.
2. Include all visual variants as separate stories.
3. Use decorators for wrapping if needed (e.g., SafeAreaProvider).
4. Run `npm run storybook` to verify visually.
